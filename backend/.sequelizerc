const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // configuração do banco
  'models-path': resolve(__dirname, 'src', 'app', 'models' ), // configuração dos models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // configuração das migrations
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // configuração das seedrs
};